# Migrations

Esta pasta é gerida automaticamente pelo Prisma CLI.

Para criar a primeira migration após configurar o `.env`:

```bash
npx prisma migrate dev --name init
```

Nunca editar os ficheiros de migration manualmente.
